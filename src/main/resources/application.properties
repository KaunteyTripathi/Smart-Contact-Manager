#spring.application.name=SmartContactManager
#
## Use Render-assigned port
#server.port=${PORT:8282}
#
## Database configuration (local MySQL)
#spring.servlet.multipart.max-file-size=10MB
#spring.datasource.url=jdbc:mysql://localhost:3306/smartcontactmanager?serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=sbit1234
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## JPA / Hibernate
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.transaction.coordinator_class=jdbc
spring.application.name=SmartContactManager

server.port=${PORT:8080}

spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect